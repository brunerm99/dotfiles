# Native Wayland renders only part of the window at 160% monitor scale.
# XWayland with an explicit scale keeps the full interface at a readable size.
--ozone-platform=x11
--force-device-scale-factor=1.25
